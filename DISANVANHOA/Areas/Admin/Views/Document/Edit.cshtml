@model DISANVANHOA.Models.EF.Document
@{
    ViewBag.Title = "Cập NhậtTài Liệu";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
@section naviheader{
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/home" class="nav-link">Trang chủ</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/Document" class="nav-link">Tài Liệu</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="#" class="nav-link">Cập Nhật</a> 
        </li>
    </ul>
}


<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Cập Nhật Tài Liệu</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Tài Liệu</a></li>
                    <li class="breadcrumb-item active">Cập Nhật</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">

    <!-- Default box -->
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Thông tin Cập Nhật Tài Liệu</h3>
        </div>
        <div class="card-body">
            <div class="bs-stepper-content">
                @using (Html.BeginForm("Edit", "Document", FormMethod.Post, new { enctype = "multipart/form-data" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true)
                    <!-- your steps content here -->
                <div id="logins-part" class="content active dstepper-block" role="tabpanel" aria-labelledby="logins-part-trigger">
                    <div class="form-group">
                        <label for="exampleInputEmail1">Tiêu đề</label>
                        @Html.TextBoxFor(x => x.Title, new { @class = "form-control", @placeholder = "Tiêu đề" })
                        @Html.ValidationMessageFor(x => x.Title, null, new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <label for="exampleInputEmail1">Alias</label>
                        @Html.TextBoxFor(x => x.Alias, new { @class = "form-control", @placeholder = "Bí danh" })
                        @Html.ValidationMessageFor(x => x.Alias, null, new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <label for="exampleInputEmail1">Địa điểm</label>
                        @Html.DropDownListFor(x => x.DocumentCategoryId, ViewBag.DoccumentCategory as SelectList, "-Chọn Địa Điểm -", new { @class = "form-control", @placeholder = "mã địa điểm" })
                        @Html.ValidationMessageFor(x => x.DocumentCategoryId, null, new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <label for="exampleInputEmail1">Chứng nhận</label>
                        @Html.DropDownListFor(x => x.CertificateId, ViewBag.Certificate as SelectList, "-Chọn chứng nhận -", new { @class = "form-control", @placeholder = " mã chứng nhận" })
                        @Html.ValidationMessageFor(x => x.CertificateId, null, new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <label for="exampleInputEmail1">Loại hình</label>
                        @Html.DropDownListFor(x => x.DocTypeId, ViewBag.DocType as SelectList, "-Chọn loại hình -", new { @class = "form-control", @placeholder = "mã loại hình" })
                        @Html.ValidationMessageFor(x => x.DocTypeId, null, new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <label for="exampleInputFile">Ảnh đại diện</label>
                        <div class="input-group">
                            <div class="custom-file">
                                @Html.TextBoxFor(x => x.Img, new { @id = "txtImage", @class = "form-control" })
                            </div>
                            <div class="input-group-append">
                                <input type="button" value="Tải ảnh" onclick="BrowseServer('txtImage');" />
                            </div>
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="exampleInputFile">Tải Tài Liệu</label>
                        <input type="file" name="file" class="form-control" />
                    </div>
                    <div class="form-group">
                        <label>Tải Video</label>
                        <input type="file" name="videoFile" accept="video/*" class="form-control" />
                    </div>
                    <div class="form-group">
                        <label>Nguồn Tài Liệu</label>
                        @Html.TextBoxFor(x => x.Author, new { @class = "form-control", @placeholder = "Nguồn tài liệu" })
                        @Html.ValidationMessageFor(x => x.Author, null, new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <label for="exampleInputPassword1">Mô tả</label>
                        @Html.TextAreaFor(x => x.Description, new { @class = "form-control" })
                    </div>
                    <div class="form-group">
                        <label for="exampleInputPassword1">Chi tiết</label>
                        @Html.TextAreaFor(x => x.Detail, new { @class = "form-control", @id = "txtDetail" })
                    </div>
                    <div class="form-group">
                        <label for="exampleInputPassword1">Hiển thị</label>
                        @Html.CheckBoxFor(x => x.IsActive, new { @checked = "@checked" })
                    </div>

                    <div class="form-group">

                        <button type="submit" class="btn btn-outline-success">Lưu</button>
                        <a href="/admin/document" class="btn btn-outline-warning">Quay lại</a>
                    </div>
                </div>

                }

            </div>
        </div>
        <!-- /.card-body -->
        <div class="card-footer">

        </div>
        <!-- /.card-footer-->
    </div>
    <!-- /.card -->

</section>
<!-- /.content -->

@section  scripts{
    <script>
          $(document).ready(function () {
              CKEDITOR.replace('txtDetail', {
                  customConfig: '/content/ckeditor/config.js',
                  extraAllowedContent: 'span',
              });
          });
          function BrowseServer(field) {
              var finder = new CKFinder();
              finder.selectActionFunction = function (fileUrl) {
                  document.getElementById(field).value = fileUrl;
              };
              finder.popup();
          }
        function handleFileSelect(event) {
            var fileInput = event.target;
            var filePath = fileInput.value;
            document.getElementById('txtFile').value = filePath; // Show the selected file path in the text box
        }
    </script>
    <script src="~/Scripts/jsConvert.js"></script>
}




